# Teardown evidence

Status: resources have not yet been deployed.

After every stage, attach the redacted destroy plan/apply output and verify that chargeable resources are absent. Explicitly check MSK clusters, RDS instances, ElastiCache replication groups, interface endpoints, NAT Gateways, and orphaned EBS snapshots. Retain the audit evidence only according to the approved demonstration policy.
